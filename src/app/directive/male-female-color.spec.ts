import { MaleFemaleColor } from './male-female-color';

describe('MaleFemaleColor', () => {
  it('should create an instance', () => {
    const directive = new MaleFemaleColor();
    expect(directive).toBeTruthy();
  });
});
